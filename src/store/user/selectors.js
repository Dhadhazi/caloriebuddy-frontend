export const selectToken = (state) => state.user.token;

export const selectActivity = (state) => state.user.activity;

export const selectBudget = (state) => state.user.budget;

export const selectConsumption = (state) => state.user.consumption;

export const selectWeight = (state) => state.user.weight;
